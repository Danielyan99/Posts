import React from 'react'
import {useAppDispatch} from "../hook/redux";

export function PostPage() {
  const dispatch = useAppDispatch()
  
  
  return (
    <div>
      post particular page
    </div>
  )
}